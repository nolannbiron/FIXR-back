import Details from '../Details/Details';

export default {
    rules: {
        type: [Details.schema],
        default: [],
    },
    extras: {
        type: [Details.schema],
        default: [],
    },
};
